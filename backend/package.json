{
  "name": "no-houchi-bicycle-net-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "test": "vitest"
  },
  "dependencies": {
    "@azure/ai-form-recognizer": "^5.1.0",
    "@azure/identity": "^4.13.0",
    "@fastify/jwt": "^6.0.0",
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^3.0.3",
    "fastify": "^4.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "eslint": "^8.47.0",
    "prettier": "^3.0.0",
    "prisma": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "vitest": "^4.0.17"
  }
}
