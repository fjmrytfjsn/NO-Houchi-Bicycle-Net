E2E Helpers

This folder can contain helper scripts to manipulate the in-memory dev store for tests.
Currently we use the `/api/owner/markers/[code]/__test__/set-eligible-past` endpoint to set the eligibleFinalAt to a past time.
