# Backend API
ADMIN_API_BASE_URL=http://localhost:3000

# Google Maps
# Used by the browser to render embedded report-location maps.
# This key is public in the built frontend; restrict it by HTTP referrer in Google Cloud.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-browser-key-here
